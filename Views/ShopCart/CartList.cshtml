@model IQueryable<ShopCart>
@{ Layout = "_LayoutOutCat";}

<table class="table">
    <tr><th>Товар</th><th>Цена</th><th> Количество</th><th></th></tr>
    @foreach (var tov in Model)
    {
<tr>
    <td>@tov.TovarName</td>
    <td>@tov.Price</td>
    <td>@tov.BuyQantity</td>
    <td>
        <form asp-action="CartDelete" asp-route-Id="@tov.Id" method="post">

            <button type="submit" class="btn btn-sm btn-danger">
                Удалить
            </button>
        </form>
    </td>
</tr>
        
    }
    
</table>
<h3>Сумма заказа: @ViewBag.Sum р</h3> <form asp-action="CartBuy"  method="post"> <button type="submit" class="btn btn-sm btn-danger">
    Заказать
</button></form>
